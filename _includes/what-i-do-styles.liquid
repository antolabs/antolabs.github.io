<style>
/* About subheadline styling */
.about-subheadline {
  font-size: 1.15rem;
  color: var(--global-text-color);
  margin-bottom: 1.5rem;
  letter-spacing: 0.01em;
}
.about-subheadline strong {
  font-weight: 600;
}

/* What I Do Section - protected from PurgeCSS */
.what-i-do-section {
  margin-top: 2rem;
  margin-bottom: 2rem;
  padding: 1.5rem;
  background-color: var(--global-code-bg-color);
  border-radius: 12px;
  border-left: 4px solid var(--global-theme-color);
}
.what-i-do-section h4 {
  margin-top: 0;
  margin-bottom: 1rem;
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--global-theme-color);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}
.what-i-do-list {
  list-style: none;
  padding: 0;
  margin: 0;
}
.what-i-do-list li {
  display: flex;
  align-items: flex-start;
  margin-bottom: 1rem;
}
.what-i-do-list li:last-child {
  margin-bottom: 0;
}
.what-i-do-icon {
  font-size: 1.5rem;
  margin-right: 1rem;
  flex-shrink: 0;
  width: 1.75rem;
  height: 1.75rem;
  display: flex;
  align-items: center;
  justify-content: center;
}
.what-i-do-icon img {
  width: 100%;
  height: 100%;
  object-fit: contain;
}
.what-i-do-content {
  flex: 1;
  font-size: 0.95rem;
  line-height: 1.5;
}
.what-i-do-content strong {
  color: var(--global-text-color);
  font-weight: 600;
}

/* Profile image alignment - align bottom with What I Do section (desktop only) */
@media (min-width: 768px) {
  .profile.float-right {
    position: relative;
    top: -4rem !important;
    margin-bottom: -4rem !important;
  }
}
</style>
